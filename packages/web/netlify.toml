# Netlify config for app.winnow.com (dashboard)
# In Netlify UI: set Base directory to packages/web for this site.

[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
